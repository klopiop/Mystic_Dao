# 阿里云 ESA Pages 环境变量示例（比赛展示用）

# OpenAI API 配置（直接在前端调用）
# 注意：这些变量会暴露在浏览器中，仅用于比赛展示
NEXT_PUBLIC_OPENAI_API_KEY=your_openai_api_key
NEXT_PUBLIC_OPENAI_API_URL=https://api.openai.com/v1/chat/completions
NEXT_PUBLIC_OPENAI_MODEL=gpt-4o-mini

# NextAuth 配置（用于前端 JWT 验证）
NEXTAUTH_SECRET=your_production_secret_key_change_me
NEXTAUTH_URL=https://your-pages-domain.aliyuncs.com
